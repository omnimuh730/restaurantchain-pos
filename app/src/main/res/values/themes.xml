<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- AppCompat parent is required for AppCompatDelegate.setApplicationLocales (per-app language). -->
    <style name="Theme.RestaurantchainPOS" parent="Theme.AppCompat.Light.NoActionBar" />
</resources>